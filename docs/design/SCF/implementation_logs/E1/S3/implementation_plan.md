# Implementation Plan - E1-S3: SCF 云端部署与 VPC 通信

## 1. Readiness Check (实施前准入)

- [x] **需求解析**: 
  - 编写 `serverless.yml`，定义 Data-Hub 函数的云端参数。
  - 配置 VPC (VPC ID / Subnet ID) 以实现内网访问现有 MySQL 5.7。
  - 整理 `requirements.txt` 并规划 Layer (层) 剥离重型依赖。
  - 解决 manylinux 依赖兼容性问题。
- [x] **依赖认证**: 
  - 确认本地已安装 `serverless` CLI (用于预览或部署)。
  - 确认腾讯云密钥 (SecretId/SecretKey) 已就绪（由用户侧持有）。
- [x] **角色激活**: 
  - 激活角色：`Cloud Architect` / `DevOps Specialist`。

## 2. 详细方案 (Proposed Changes)

### 2.1 基础设施配置 [NEW]
- **`scf-collector/serverless.yml`**: 
  - 使用 `component: scf` 声明。
  - 设置 `runtime: Python3.10`。
  - 注入 `environment` 变量 (从 `.env` 映射)。
  - 配置 `vpcConfig` 确保与数据库内网互通。
- **`scf-collector/requirements.txt`**: 列出所有核心依赖 (`aiomysql`, `mootdx`, `akshare`, `pandas`, `tushare`)。

### 2.2 部署策略 (Deployment Strategy)
1. **Layer 构建**: 建议通过 `pip install ... -t ./layers/python` 将依赖打包，并上传为腾讯云 SCF Layer。
2. **环境变量保护**: 敏感 Token 使用 SCF 环境变量管理，不在 `serverless.yml` 中明文显示。

## 3. 任务列表 (Tasks)
- [ ] T1: 整理 `requirements.txt` 并创建项目依赖清单。
- [ ] T2: 编写 `serverless.yml` 完整配置。
- [ ] T3: 编写 `.env.example` 模板，定义云端所需的变量。
- [ ] T4: [可选] 协助用户进行本地部署尝试 (或生成部署脚本)。

## 4. 验收计划 (Verification)
- **AC1 (Network Connectivity)**: 
  - 使用 `serverless invoke` (或在控制台点击测试) 执行 `Data-Hub`。
  - **确认返回结果非「Connection Timeout」，而是正常的入库成功 JSON。**
- **AC2 (Resource Efficiency)**: 
  - 在腾讯云控制台查看监控，确认函数运行时的峰值内存 < 256MB。

---
*Generated by Antigravity AI - 2026-05-11*
