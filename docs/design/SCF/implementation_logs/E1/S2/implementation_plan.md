# Implementation Plan - E1-S2: 任务流水审计与数据库插库 (Idempotency)

## 1. Readiness Check (实施前准入)

- [x] **需求解析**: 
  - 实现基于 `aiomysql` 的高性能异步数据库连接层，并针对 Serverless 环境加入连接重试/复用逻辑。
  - 将 E1-S1 采集到的标准化 K 线数据安全存入 `stock_kline_daily`。
  - 同步写入任务流水 (`pipeline_run`) 和数据就绪状态 (`data_readiness`)。
  - **任务完成/失败时，触发标准邮件报告（遵照 `AGENTS.md` 调度规范）。**
  - 实现幂等插入 (`ON DUPLICATE KEY UPDATE`) 防止数据重复。
- [x] **依赖认证**: 
  - 库依赖：需要引入 `aiomysql` 和 `pydantic`。
  - Schema：对齐 `TABLES_INDEX.md`。
- [x] **角色激活**: 
  - 激活角色：`Python Backend Engineer` / `Database Expert`。

## 2. 详细方案 (Proposed Changes)

### 2.1 新增数据库访问层 (DAO) [NEW]
- **`scf-collector/shared/db/connection.py`**: 
  - 封装 `aiomysql.create_pool`，实现 Serverless 友好的数据库单例连接池。
  - 捕获 `OperationalError` (如 Broken Pipe) 并执行自动重试。
- **`scf-collector/shared/db/dao.py`**:
  - `save_kline_data`: 批量执行 `INSERT INTO stock_kline_daily ... ON DUPLICATE KEY UPDATE`。
  - `log_pipeline_run`: 写入审计表。
  - `update_data_readiness`: 更新探测表状态为 `ready`。
- **`scf-collector/shared/utils/notifier.py`** [NEW]:
  - `send_email_report`: 封装基于 SMTP 的邮件发送模块，报告采集量、耗时及失败信息。

### 2.2 核心调度整合 [MODIFY]
- **`scf-collector/functions/data_hub/index.py`**:
  - 在成功获取采集数据后，调用 DAO 层的三个存储方法完成入库。

## 3. 任务列表 (Tasks)
- [ ] T1: 编写 `connection.py`，实现支持防断连的 Async 连接池。
- [ ] T2: 编写 `dao.py`，实现幂等插入逻辑与审计流水更新逻辑。
- [ ] T3: 编写 `notifier.py`，实现任务完成/失败的标准邮件通知。
- [ ] T4: 修改 `data_hub/index.py`，接入数据库写入与通知流程。
- [ ] T5: 编写联调测试脚本验证入库与邮件投递。

## 4. 验收计划 (Verification)
- **物理查验 (True Source)**: 
  - 编写并运行 `scf-collector/tests/test_db_insert.py`，模拟一条伪造行情写入。
  - **直接通过 `docker exec` 或 SQL 客户端进入真实的 MySQL 5.7 库中查询 `stock_kline_daily`、`pipeline_run` 及 `data_readiness`，截图或打印真实 SQL 返回内容留存至 `walkthrough.md`。**
- **AC 校验**: 
  - 二次运行插入同样的数据，检查返回的 `affected_rows`，确认 `ON DUPLICATE KEY UPDATE` 已生效。

---
*Generated by Antigravity AI - 2026-05-11*
