# Implementation Plan - E1-S1: Data-Hub 核心采集适配器实现

## 1. Readiness Check (实施前准入)

- [x] **需求解析**: 
  - 实现一个名为 `Data-Hub` 的云函数入口。
  - 支持 `Tushare`、`AkShare` 和 `mootdx` 三种数据源的适配。
  - 统一输出符合项目规范的 K 线 JSON 结构。
- [x] **依赖认证**: 
  - 确认 `TABLES_INDEX.md` 中已定义 `stock_kline_daily` (行 165)。
  - 环境：Python 3.10, 腾讯云 SCF。
- [x] **角色激活**: 
  - 激活角色：`Python Backend Engineer` / `Data Infrastructure Expert`。

## 2. 详细方案 (Proposed Changes)

### 2.1 隔离代码结构 [NEW]
- **`scf-collector/shared/collectors/base.py`**: 定义抽象基类 `BaseCollector`。
- **`scf-collector/shared/collectors/mootdx_cl.py`**: mootdx 适配器实现。
- **`scf-collector/shared/collectors/akshare_cl.py`**: AkShare 适配器实现。
- **`scf-collector/shared/collectors/tushare_cl.py`**: Tushare 适配器实现。
- **`scf-collector/functions/data-hub/index.py`**: SCF 入口函数，负责分发请求。

### 2.2 逻辑设计
1. **适配器模式**: 每一个数据源子类实现 `fetch_daily_kline` 方法，基类采用 `async def` 定义以遵循 Async First 规范（同步库在内部使用线程池或异步库封装）。
2. **数据标准化**: 统一将各源返回的 DataFrame/List 转换为字典列表，字段名对齐 `stock_kline_daily` 的 schema（如 `pct_chg` 必须为小数）。
3. **依赖分离 (Layer)**: 规划将 Pandas、mootdx 等重型依赖打包为 SCF Layer，以保证业务代码体积 < 50MB。

## 3. 任务列表 (Tasks)
- [ ] T1: 创建抽象基类与标准化 Schema 定义。
- [ ] T2: 实现 mootdx 数据源适配器。
- [ ] T3: 实现 AkShare 数据源适配器。
- [ ] T4: 实现 Tushare 数据源适配器（含 Token 注入）。
- [ ] T5: 编写 SCF `main_handler` 调度逻辑。
- [ ] T6: 单元测试验证 AC1 & AC2。

## 4. 验收计划 (Verification)
- **物理查验 (True Source)**: 在 `scf-collector/` 下执行测试脚本触发采集后，必须通过直连现有 MySQL 数据库，执行 SQL 查询以确认数据真实落库，并在 `walkthrough.md` 中提供真实 SQL 结果证明。
- **AC 校验**: 检查库中保存的 `ts_code` 格式是否规范，`pct_chg` 是否已经除以 100 转换为小数。

---
*Generated by Antigravity AI - 2026-05-11*
