# A 股盘后数据观察体系 · 数据需求与指标规范

## 背景

为支持 A 股盘后行情复盘,需建立一套结构化的**原始数据采集需求**与**派生指标输出规范**,作为后续数据接入与计算实现的输入。

## 目标

- 明确盘后分析所需的原始数据类型、覆盖范围、更新频率与数据时效
- 定义最终对外输出的指标清单(分层、字段、含义)
- 沉淀实施过程中的关键风险与避坑要点

## 范围

- 单日盘后(15:00 之后)可获取的市场数据,以日频为主
- A 股全市场(沪深北),以及与 A 股决策强相关的港股、海外、商品、汇率、利率数据

## 非目标

- 不覆盖日内 Tick / 分钟级实时数据采集
- 不涉及量化策略回测、信号生成与下单执行
- 不涉及具体数据源选型与接口实现(由实施阶段决定)

---

## E1:原始数据采集需求

### E1-S1:行情数据

**作为** 盘后分析系统,**我希望** 获取全市场当日行情快照,**以便** 计算市场全景与结构指标。

#### 需要的数据

| 数据 | 维度 | 关键字段 | 时效 |
|---|---|---|---|
| 全 A 股票日线 K 线 | 个股 × 日期 | 开/高/低/收、成交量、成交额、换手率、振幅、量比、复权因子 | T 日 15:30 后 |
| 核心指数日线 | 指数 × 日期 | 收盘、涨跌幅、成交额 | T 日 15:30 后 |
| 申万一级 / 二级行业行情 | 行业 × 日期 | 收盘、涨跌幅、成交额、领涨股、上涨家数 | T 日 16:00 后 |
| 中信一级行业行情 | 行业 × 日期 | 同上(用于交叉验证) | T 日 16:00 后 |
| 概念板块 / 题材行情 | 概念 × 日期 | 涨跌幅、成交额、成分股、领涨股 | T 日 16:00 后 |
| 涨跌停清单 | 个股 × 日期 | 涨/跌停状态、首次封板时间、封单金额、封板次数 | T 日 15:30 后 |

#### 覆盖范围要求

- 沪市主板 + 科创板 + 深市主板 + 创业板 + 北交所
- 核心指数至少包括:上证综指、深证成指、创业板指、科创 50、北证 50、沪深 300、中证 500、中证 1000、中证 2000、万得全 A
- 行业分类以**申万一级 31 个**为主,中信作为交叉验证
- 复权方式需同时支持:不复权(估值用)、前复权(走势分析用)、后复权(回测用)

#### AC

- Given T 日 18:00,When 触发当日采集,Then 全 A 股票数据完整入库,缺失率 < 0.1%
- Given 任一交易日,When 查询某指数当日数据,Then 涨跌幅与交易所披露一致(误差 < 0.01%)
- Given 涨跌停清单,When 校验,Then 与交易所披露的涨跌停股票数量完全一致

---

### E1-S2:资金流数据

#### 需要的数据

| 数据 | 维度 | 关键字段 | 时效 |
|---|---|---|---|
| 个股资金流 | 个股 × 日期 | 主力净流入、超大单/大单/中单/小单净流入、净流入占比 | T 日 16:00 后 |
| 行业资金流 | 行业 × 日期 | 主力净流入、净流入占比、领涨股资金流 | T 日 16:00 后 |
| 概念资金流 | 概念 × 日期 | 主力净流入、净流入占比 | T 日 16:00 后 |
| 大盘资金流 | 全市场 × 日期 | 全市场主力净流入、超大单净流入 | T 日 16:00 后 |
| 北向资金日终数据 | 全市场 × 日期 | 当日净买入、累计净买入 | T 日 16:30 后 |
| 两融数据 | 全市场 / 行业 / 个股 × 日期 | 融资余额、融券余额、融资买入额、融资偿还额 | T+1 日 9:30 前 |

#### 关键说明

- 主力资金算法各家口径不一致,**实施时固定一个口径长期使用**,不同口径不可混合统计
- **港交所自 2024-08-19 起停止披露北向盘中实时数据,仅日终披露成交净额**,北向数据已退化为日度参考
- 两融数据由交易所次日披露,T 日数据需 T+1 日上午回填

#### AC

- Given T 日 19:30,When 采集资金流,Then 主力资金流字段完整,空值率 < 1%
- Given 北向资金,When 采集,Then 仅采集日终披露值,不依赖任何盘中实时数据
- Given 两融数据,When T+1 日上午回填,Then 与交易所披露完全一致

---

### E1-S3:龙虎榜与大宗交易

#### 需要的数据

| 数据 | 维度 | 关键字段 | 时效 |
|---|---|---|---|
| 龙虎榜个股明细 | 个股 × 日期 | 上榜原因、买卖营业部、买卖金额、净买入 | T 日 17:30 后 |
| 龙虎榜营业部明细 | 营业部 × 日期 × 股票 | 营业部代码、营业部名称、买入/卖出额 | T 日 17:30 后 |
| 大宗交易记录 | 个股 × 日期 | 成交价、成交量、成交额、折/溢价率、买/卖营业部 | T 日 18:00 后 |

#### 配套数据要求

- **游资席位映射库**:维护一份 `(营业部代码 → 游资名称)` 的本地字典,定期更新。常见知名游资席位需被识别(如"拉萨团结路"、"宁波桑田路"、"上海溧阳路"等)
- **机构席位识别**:营业部名称中包含"机构专用"或"机构席位"的需单独标记

#### AC

- Given T 日 18:30,When 采集龙虎榜,Then 当日全部上榜股票及营业部明细入库
- Given 营业部代码,When 匹配映射库,Then 知名游资识别率 > 80%

---

### E1-S4:涨停板与情绪数据

#### 需要的数据

| 数据 | 维度 | 关键字段 | 时效 |
|---|---|---|---|
| 涨停池 | 个股 × 日期 | 首次涨停时间、最后封板时间、连板数、封板资金、所属概念 | T 日 15:30 后 |
| 跌停池 | 个股 × 日期 | 首次跌停时间、连续跌停数、封板资金 | T 日 15:30 后 |
| 炸板池 | 个股 × 日期 | 炸板次数、封板时间、最高涨幅、收盘涨幅 | T 日 15:30 后 |
| 连板池 | 个股 × 日期 | 当前连板高度、连板天数、所属概念 | T 日 15:30 后 |
| 强势股 / 弱势股 | 个股 × 日期 | 涨速、5 日涨幅、20 日涨幅、突破信号 | T 日 15:30 后 |

#### AC

- Given T 日 16:00,When 查询涨停池,Then 涨停股数量与全 A 行情数据中 `pct_change >= 9.9%` 且封板的股票数一致
- Given 连板高度,When 计算,Then 与历史涨停记录递推一致

---

### E1-S5:ETF 与基金数据

#### 需要的数据

| 数据 | 维度 | 关键字段 | 时效 |
|---|---|---|---|
| ETF 日终行情 | ETF × 日期 | 收盘价、净值、折/溢价率、成交额 | T 日 16:00 后 |
| ETF 份额变化 | ETF × 日期 | 当日总份额、份额变化、净申购/赎回份额、净申购/赎回金额 | T+1 日 9:30 前 |
| 主动基金净值(可选) | 基金 × 日期 | 单位净值、累计净值 | T+1 日 |

#### 重点关注 ETF 清单

- **国家队信号 ETF**:大型沪深 300 ETF、上证 50 ETF、科创 50 ETF(汇金及大型机构集中持有的几只)
- **宽基 ETF**:沪深 300、中证 500、中证 1000、创业板、科创 50、北证 50
- **行业主题 ETF**:覆盖申万一级所有行业的代表性 ETF

#### AC

- Given 国家队 ETF 清单,When 计算份额变化,Then 异常大额申购可被识别(z-score 超过阈值)

---

### E1-S6:估值数据

#### 需要的数据

| 数据 | 维度 | 关键字段 | 时效 |
|---|---|---|---|
| 个股估值 | 个股 × 日期 | PE-TTM、PB、PS-TTM、股息率、市值 | T 日 18:00 后 |
| 指数估值 | 指数 × 日期 | PE-TTM、PB、股息率(算术 / 加权) | T 日 18:00 后 |
| 行业估值 | 申万一级 × 日期 | PE-TTM、PB、股息率 | T 日 18:00 后 |
| 历史估值序列 | 同上 × 至少 10 年 | 用于分位数计算 | 一次性回补 |

#### AC

- Given 个股 PE-TTM,When 计算,Then 基于最新 4 个季度归母净利润求和(不含非经常损益剔除版本)
- Given 历史序列长度,When 系统上线,Then 至少回补 10 年历史(2014 年起)

---

### E1-S7:基本面与公告数据

#### 需要的数据

| 数据 | 维度 | 关键字段 | 时效 |
|---|---|---|---|
| 业绩预告 | 个股 × 公告日 | 预告类型、净利润下限/上限、同比增长 | 实时(以公告披露为准) |
| 业绩快报 | 个股 × 公告日 | 营收、净利润、同比 | 实时 |
| 定期财报披露 | 个股 × 公告日 | 一季报 / 半年报 / 三季报 / 年报 | 实时 |
| 解禁数据 | 个股 × 解禁日 | 解禁数量、解禁市值、解禁类型 | 提前披露 |
| 股东增减持 | 个股 × 公告日 | 增减持类型、数量、比例、价格区间 | 实时 |
| 回购公告 | 个股 × 公告日 | 回购类型、金额上限、进度 | 实时 |
| ST / 退市风险 | 个股 × 公告日 | 警示类型、原因 | 实时 |
| 立案调查 / 监管处罚 | 个股 × 公告日 | 类型、原因 | 实时 |
| 新股日历 | 个股 × 申购/上市日 | 申购代码、发行价、上市日期 | 提前披露 |
| 分红送转 | 个股 × 公告日 | 分红方案、除权除息日 | 实时 |

#### AC

- Given 业绩预告,When 采集,Then 包含预告类型分类(预增 / 预减 / 略增 / 略减 / 扭亏 / 首亏 / 续亏 / 续盈 / 不确定)
- Given 解禁日历,When 查询未来 30 天,Then 完整列出所有解禁事件及市值

---

### E1-S8:跨市场与海外数据

#### 需要的数据

| 数据 | 维度 | 关键字段 | 时效 |
|---|---|---|---|
| 港股核心指数 | 指数 × 日期 | 恒生指数、恒生科技、恒生国企收盘 | T 日 16:30 后 |
| 南向资金 | 全市场 × 日期 | 当日净买入、累计净买入 | T 日 16:30 后 |
| AH 溢价指数 | 全市场 × 日期 | 溢价指数值 | T 日 16:30 后 |
| 大宗商品 | 品种 × 日期 | 原油(布伦特 / WTI)、铜(LME)、黄金(COMEX)、铁矿、动力煤、碳酸锂、多晶硅 | T 日 16:00 后 / 海外另行 |
| 汇率 | 货币对 × 日期 | USD/CNY 在岸、USD/CNY 离岸、中间价 | T 日 |
| 利率 | 期限 × 日期 | 中国 10Y 国债、美国 10Y 国债、中美利差、Shibor、DR007 | T 日 |
| 美股核心指数 | 指数 × 日期 | 标普 500、纳斯达克、纳斯达克金龙(中概) | T 日(美股前一交易日) |
| 主要中概股 ADR | 个股 × 日期 | 阿里、腾讯 ADR、京东、拼多多等 | T 日 |

#### AC

- Given 海外市场,When 与 A 股做次日联动分析,Then 美股以 A 股 T 日的"美东 T-1 收盘"为准
- Given 港股,When A 股交易日是港股休市日,Then 标记数据缺失,不做异常告警

---

### E1-S9:衍生品数据

#### 需要的数据

| 数据 | 维度 | 关键字段 | 时效 |
|---|---|---|---|
| 股指期货 | 合约 × 日期 | IF / IH / IC / IM 当月 + 次月合约的收盘、基差、持仓量 | T 日 16:00 后 |
| ETF 期权 | 合约 × 日期 | 50ETF、300ETF、500ETF、1000ETF 期权的 IV、PCR(成交量 / 持仓量) | T 日 16:30 后 |

#### AC

- Given 股指期货基差,When 计算年化基差率,Then 公式为 `(期货价 - 现货价) / 现货价 / 剩余天数 × 365`

---

## E2:派生指标输出规范

> 所有指标按盘后看盘的 9 个观察层组织,字段命名遵循 `层级_指标名` 约定。涨跌幅以小数表示(0.0123 = 1.23%)。

### E2-S1:L1 市场全景指标

```yaml
指标组: ads_market_overview_daily
主键: trade_date
指标:
  # 指数表现
  - 各核心指数收盘 + 涨跌幅(上证、深证、创业板、科创50、北证50、沪深300、500、1000、2000、万得全A)

  # 成交活跃度
  - turnover_total                # 全市场成交额
  - turnover_ma5 / ma20           # 5日 / 20日均值
  - turnover_pct_vs_ma20          # 相对 20 日均值倍数
  - turnover_pctile_1y            # 近 1 年分位数

  # 涨跌家数
  - up_count / down_count / flat_count
  - up_down_ratio                 # 涨跌比
  - limit_up_count / limit_down_count
  - high_60d_count / low_60d_count
  - market_breadth                # 市场宽度 = 涨家数 / 总家数

  # 换手与量比
  - turnover_rate_avg             # 全市场加权换手率
  - volume_ratio_avg              # 平均量比

  # 派生分类
  - market_regime                 # broad_up / broad_down / structural / low_vol
```

---

### E2-S2:L2 结构分化指标

```yaml
指标组: ads_style_factor_daily
主键: trade_date
指标:
  - large_vs_small                # 沪深300 - 中证2000(大小盘强弱)
  - value_vs_growth               # 300价值 - 300成长
  - dividend_vs_micro             # 中证红利 - 万得微盘股
  - momentum_factor               # 近20日动量分组多空收益

指标组: ads_industry_sw1_daily
主键: (trade_date, industry_code)
指标:
  - industry_code / industry_name # 申万一级 31 个
  - pct_change / turnover / main_net_inflow
  - up_count / down_count
  - leader_stock_code / leader_pct # 领涨股
  - rank_today / rank_5d / rank_20d
  - rank_diff_5d                  # 排名变化(动量)
  - pe_ttm / pb / dividend_yield
  - pe_pctile_5y / pb_pctile_5y

指标组: ads_concept_daily
主键: (trade_date, concept_code)
指标:
  - concept_code / concept_name
  - pct_change / turnover / main_net_inflow
  - constituent_count / up_count
  - top3_leaders                  # 领涨股 Top3
  - persistence_score             # 与昨日 / 5 日榜单重叠度
```

---

### E2-S3:L3 资金层指标

```yaml
指标组: ads_capital_flow_daily
主键: trade_date
指标:
  # 主力资金
  - main_inflow_total             # 全市场主力净流入
  - main_inflow_top3_industry     # 主力流入行业 Top3
  - main_outflow_top3_industry

  # 两融
  - margin_balance                # 两融余额
  - margin_balance_diff           # 较前日变化
  - margin_buy_amount             # 当日融资买入额
  - margin_buy_pct_of_turnover    # 占成交比

  # 北向(日终)
  - north_net_inflow              # 北向净流入(日终)
  - north_net_inflow_5d_sum

  # ETF
  - etf_broad_net_subscription    # 宽基 ETF 净申购份额
  - etf_huijin_signal             # 国家队 ETF 异常申购信号(bool)
  - etf_industry_top3_subscription
  - etf_industry_top3_redemption

  # 龙虎榜
  - lhb_inst_net_buy_top          # 机构席位净买入 Top5
  - lhb_inst_net_sell_top
  - lhb_yz_net_buy_top            # 知名游资净买入 Top5

  # 大宗交易
  - block_trade_total             # 大宗成交额
  - block_trade_avg_discount      # 平均折/溢价率

  # 期指基差
  - if_basis / ih_basis / ic_basis / im_basis  # 当月合约年化基差
  - ic_basis_20d_chg              # IC 基差近 20 日变化(对冲情绪)
```

---

### E2-S4:L4 情绪层指标

```yaml
指标组: ads_sentiment_daily
主键: trade_date
指标:
  # 连板梯队
  - first_board_count             # 首板数
  - second_board_count
  - max_board_height              # 最高板高度
  - board_ladder                  # 各板高度数量分布(JSON)

  # 晋级率
  - promotion_rate                # 加权晋级率
  - promotion_rate_2to3           # 二进三晋级率

  # 炸板与封板
  - blast_count                   # 炸板数
  - blast_rate                    # 炸板率 = 炸板数 / (涨停 + 炸板)
  - seal_money_avg                # 平均封单金额

  # 赚钱效应
  - yesterday_zt_avg_return       # 昨日涨停今日平均涨跌幅
  - yesterday_zt_red_rate         # 昨日涨停今日红盘率
  - yesterday_zt_continuation_rate # 昨日涨停今日仍涨停率
  - profit_effect_score           # 综合赚钱效应得分

  # 异象
  - one_word_zt_count             # 一字板数
  - sky_to_ground_count           # 天地板数
  - ground_to_sky_count           # 地天板数

  # 中期情绪
  - csi300_dy_minus_10y           # 沪深300股息率 - 10年国债
  - erp_wind_a                    # 万得全A风险溢价
  - erp_pctile_10y                # ERP 10 年分位数
```

#### 关键计算口径

```python
# 晋级率
promotion_rate_2to3 = 今日二板数 / 昨日首板数

# 炸板率
blast_rate = 炸板数 / (涨停数 + 炸板数)

# ERP 风险溢价
erp = 1 / pe_ttm_wind_a - yield_10y_pct / 100
# 近 10 年分位数越高 → 股票相对债券越便宜
```

---

### E2-S5:L5 估值层指标(每周更新)

```yaml
指标组: ads_valuation_weekly
主键: (trade_date, target_code)
指标:
  - target_type                   # index / sw1 / sw2
  - target_code / target_name
  - pe_ttm / pb / ps_ttm / dividend_yield
  - pe_pctile_5y / pe_pctile_10y
  - pb_pctile_5y / pb_pctile_10y
  - dy_pctile_10y
  - pb_below_1_ratio              # 行业内破净率(仅行业适用)
  - valuation_signal              # cheap / fair / expensive
```

---

### E2-S6:L6 基本面与公告指标

```yaml
指标组: ads_fundamental_events_daily
主键: (trade_date, event_id)
指标:
  - event_type                    # yjyg(预告) / yjkb(快报) / jiejin(解禁) / huigou(回购) / jianchi(增减持) / st / lian(立案)
  - stock_code / stock_name / industry_sw1
  - event_summary                 # 关键字段摘要
  - magnitude                     # 业绩增长率 / 解禁市值 / 减持比例等
  - is_above_expectation          # 是否超预期(基于一致预期对比,可选)
  - next_day_attention_score      # 次日关注度评分(规则打分)

指标组: ads_calendar_upcoming
主键: (trade_date, event_date, event_type)
指标:
  - event_date / event_type       # macro / policy / earnings / jiejin / ipo
  - description
  - magnitude                     # 解禁市值 / IPO 募资额等
```

---

### E2-S7:L7 跨市场指标

```yaml
指标组: ads_cross_market_daily
主键: trade_date
指标:
  # 港股
  - hsi_close / hsi_pct           # 恒生指数
  - hstech_close / hstech_pct     # 恒生科技
  - south_net_inflow              # 南向资金
  - ah_premium                    # AH 溢价指数

  # 商品
  - brent_close / brent_pct
  - lme_copper_close / lme_copper_pct
  - comex_gold_close / comex_gold_pct
  - iron_ore_close / iron_ore_pct

  # 汇率与利率
  - usdcny_onshore / usdcny_offshore
  - cny_diff_on_off               # 在离岸价差(资本流动信号)
  - cn_10y_yield / us_10y_yield
  - cn_us_10y_spread              # 中美利差
  - shibor_overnight / dr007

  # 美股(供次日参考,以美东前一交易日收盘为准)
  - sp500_close_pct
  - ndx_close_pct
  - dragon_index_pct              # 纳斯达克金龙
```

---

### E2-S8:L8 异动与个股指标

```yaml
指标组: ads_stock_anomaly_daily
主键: (trade_date, stock_code)
指标:
  - stock_code / stock_name / industry_sw1
  - anomaly_type                  # top_gainer / top_loser / high_turnover / volume_spike / breakout
  - pct_change / turnover_rate / volume_ratio
  - main_net_inflow
  - lhb_flag                      # 是否登榜
  - concept_tags                  # 所属概念
  - board_height                  # 当前连板数(若涨停)
  - notes                         # 自动生成的描述
```

---

### E2-S9:L9 次日决策准备

```yaml
指标组: ads_next_day_brief
主键: trade_date
指标:
  - hot_themes_today              # 今日强势主线 + 龙头梯队
  - watchlist_continuation        # 接力候选(高位连板 + 一字板)
  - watchlist_breakout            # 技术突破标的
  - watchlist_earnings            # 次日财报公布标的
  - calendar_next_day             # 次日重要事件
  - calendar_next_week            # 本周重要事件
  - market_summary_text           # 自动生成的复盘文字(模板填充)
```

---

## 风险与避坑

### 数据采集类

1. **接口字段漂移**:数据源字段名经常微调,采集层必须有**字段映射层 + 单元测试**,避免上游变更导致下游计算静默错误
2. **限频与封 IP**:爬取类接口高频访问会被限制,采集层必须有 **sleep + 重试 + 退避策略**,必要时加代理池
3. **数据时效错位**:不同数据更新时间不同(行情 15:30、龙虎榜 17:30、ETF 份额 19:00、两融 T+1),**主任务建议 T 日 19:30 启动,部分数据 T+1 上午回填**
4. **同源多口径差异**:主力资金、行业归类等不同源算法不同,**固定一个口径长期使用**,跨源对比无意义
5. **节假日处理**:必须使用**交易所交易日历**而非自然日历;港股 / A 股 / 美股节假日不一致,跨市场指标要单独处理缺失日
6. **北向数据已退化**:2024-08-19 后港交所停止披露盘中实时数据,北向只能日度参考,**任何依赖盘中北向数据的设计需废弃**

### 数据质量类

7. **行业分类历史兼容**:**申万一级 2021-12-13 从 28 改为 31 个**,处理跨期数据需用代码映射保持一致性
8. **复权口径混用**:估值用不复权、走势用前复权、回测用后复权,**严禁混用**
9. **ST 与退市处理**:全市场指标计算时需明确是否剔除 ST、*ST、退市过渡期股票,口径要文档化
10. **新股次新股失真**:上市未满 60 日的新股波动失真,部分指标(连板梯队、赚钱效应、振幅排行)应剔除
11. **B 股与停牌股**:计算"全 A"指标时统一剔除 B 股、长期停牌股、退市股,口径要写死
12. **复权因子滞后**:个股发生分红送转时,历史复权因子会被回溯调整,**已落库历史数据需定期校验更新**

### 指标计算类

13. **历史基线长度**:分位数类指标(估值分位、ERP 分位、成交分位)至少需要 **3 年以上**历史数据,**5–10 年最佳**;系统上线必须先回补
14. **极端值污染**:部分指标(行业 PE)会被亏损股严重扭曲,需用**中位数代替均值**,或剔除异常值后再聚合
15. **样本变化**:指数成分、行业成分会调整,历史回溯时需用**当时的成分**而非当前成分,否则有幸存者偏差
16. **政策与制度变化**:涨跌停规则改变(如科创板 / 创业板 20%)、T+0 试点、注册制等,跨期对比时需注意制度断点
17. **季节性**:财报季(1月、4月、8月、10月)业绩公告密集,事件类指标会出现规律性高峰,不要误判为异常

### 工程类

18. **幂等性保证**:采集与计算任务要可重跑,主键 + upsert,防止重复执行造成数据脏写
19. **数据血缘**:派生指标必须可追溯到原始数据日期、源、采集时间戳,出问题能快速定位
20. **告警机制**:采集失败、数据空值率超阈值、关键指标突变(如成交额单日翻倍)需自动告警,不能静默
21. **指标基线建立**:所有相对类指标(高 / 低 / 异常)的判断阈值必须**基于历史分布**而非主观经验值,且需要随时间滚动更新

---

## 技术依赖

- 关系型数据库或列式存储(承载结构化指标表)
- 对象存储或本地 Parquet(承载原始行情快照)
- 任务调度系统(支持依赖编排与重试)
- 监控告警系统(数据质量与采集状态)

## 里程碑

- **M1**:E1-S1 ~ E1-S4 数据接入完成,可输出 L1 + L3 + L4 指标(MVP)
- **M2**:E1-S5 ~ E1-S7 接入,补齐 ETF / 估值 / 公告,完整 9 层指标
- **M3**:E1-S8 ~ E1-S9 接入,跨市场与衍生品全覆盖
- **M4**:历史数据回补至 10 年,所有分位数指标达到稳态

## 度量指标

- 数据采集成功率 > 99.5%
- 关键指标计算延迟 < T 日 20:00(行情类) / T+1 日 10:00(财报类)
- 历史数据完整性:近 5 年缺失日 < 5 天
- 字段空值率:核心指标 < 0.5%,扩展指标 < 5%
